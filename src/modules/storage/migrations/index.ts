import { initial } from './001_initial';
import { type Migration } from './types';

/** Append new migrations here; record each in docs/progress.md. */
export const MIGRATIONS: readonly Migration[] = [initial];
